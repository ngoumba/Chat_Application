<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ucad.esp.dgi.chatapplication">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatApplication">
        <activity
            android:name=".Vue.ItemChatActivity"
            android:exported="false" />
        <activity
            android:name=".Vue.ChatActivity"
            android:exported="false" />
        <activity
            android:name=".Vue.ItemUserActivity"
            android:exported="false" />
        <activity
            android:name=".Vue.ContactsActivity"
            android:exported="false" />
        <activity
            android:name=".Vue.Main"
            android:exported="false" />
        <activity
            android:name=".Vue.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>